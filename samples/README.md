# Sample pack

`rectangular-carton-80x120x35.svg` is a synthetic, redistributable 1:1 vector sample. `detection-overlay.json` demonstrates normalized annotation coordinates and confidence metadata. The in-app guided sample creates a synthetic package image procedurally; no third-party reference image is bundled.
